import React from 'react';
interface NotFoundProps {
    header?: React.ReactNode;
    code?: number;
    content?: React.ReactNode;
    action?: React.ReactNode;
    sidebarWidth?: number;
}
export default function SideContent({ content, sidebarWidth }: NotFoundProps): any;
export {};
